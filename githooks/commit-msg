#!/bin/sh
# Enforce project policy: no Co-authored-by trailers, ever.
# Strips them from the commit message in place.
if grep -q "^Co-authored-by:" "$1"; then
  grep -v "^Co-authored-by:" "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
